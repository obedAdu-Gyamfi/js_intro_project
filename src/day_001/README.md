# Day 1: Runtime, Output, and Variables

Create a student profile using `console.log`, `const`, and `let`. Run it with:

```bash
node src/day_001/index.js
```

Stretch goal: move the profile formatting into a reusable function.
